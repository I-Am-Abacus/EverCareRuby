<% provide(:title, 'All users (admin only)') %>
<% provide(:navbar_h1, 'All users (admin only)') %>
<h1>All users (admin only)</h1>

<%= will_paginate %>

<ul class="users">
  <%= render @users %>
</ul>

<%= will_paginate %>
