require 'spec_helper'

describe "Follows" do
  describe "GET /follows" do
    it "works! (now write some real specs)" do
      visit follows_path
      response.status.should be(200)
    end
  end
end
